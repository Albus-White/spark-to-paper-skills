# Legacy Migration Specification

`lifecycle.py migrate-legacy` imports `story.json`, `blueprint.json`, `results.facts.json`, and `proposal.md` as `IMPORTED_UNVERIFIED` intake artifacts. Migration never grants a passing gate. The active Idea, claims, contract, repositories, environment and evidence must be registered through the new lifecycle before formal empirical claims are allowed.
