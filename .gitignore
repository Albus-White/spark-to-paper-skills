# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
# Environments
.venv/
venv/
.env
# OS / editor
.DS_Store
Thumbs.db
*.swp
# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
# Per-paper run outputs (generated, not source)
*_run/

# Official PaperBanana is PULLED, not vendored (skills/ts-figure-svg/scripts/setup_paperbanana.py).
skills/ts-figure-svg/engine/

# Vendored DrawAI engine: runtime (models/venv) + skill run outputs are GENERATED, not source.
# (Paths are repo-root-anchored — a bare 'ts-figure-optimize/…' would never match under skills/.)
skills/ts-figure-optimize/engine/.local/
skills/ts-figure-optimize/engine/.venv/
skills/ts-figure-optimize/engine/**/__pycache__/
skills/ts-figure-optimize/runs/

# extracted KG working copy (source of truth = kg/kg_ai.rar via LFS)
kg/kg_ai/

# Generated outputs & process artifacts (not source) — don't track/push
outputs/
docs/superpowers/
docs/promo/
